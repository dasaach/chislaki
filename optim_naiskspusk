#include <iostream>
#include <cmath>

using namespace std;

const double EPS = 0.5;
const double ALPHA = 0.1;

struct Point
{
    double x1, x2;
};

double f(Point x)
{
    return x.x1*x.x1 +
           4*x.x2*x.x2 +
           x.x1*x.x2 +
           x.x1;
}

Point grad(Point x)
{
    Point g;

    g.x1 = 2*x.x1 + x.x2 + 1;
    g.x2 = x.x1 + 8*x.x2;

    return g;
}

double norm(Point g)
{
    return sqrt(g.x1*g.x1 + g.x2*g.x2);
}

int main()
{
    Point x = {3,1};

    double prevNorm = 0;

    int k = 0;

    while(true)
    {
        Point g = grad(x);

        double n = norm(g);

        if(n < EPS)
            break;

        // движение ПРОТИВ градиента
        x.x1 = x.x1 - ALPHA*g.x1;
        x.x2 = x.x2 - ALPHA*g.x2;

        double error =
            fabs(n - prevNorm)/2.0;

        cout << "Iter = " << k+1 << endl;

        cout << "x1 = " << x.x1 << endl;
        cout << "x2 = " << x.x2 << endl;

        cout << "f(x) = "
             << f(x) << endl;

        cout << "||grad|| = "
             << n << endl;

        cout << "error = "
             << error << endl;

        cout << "------------------"
             << endl;

        prevNorm = n;

        k++;
    }

    return 0;
}
